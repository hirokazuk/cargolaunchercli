{
  "name": "cargolauncher2",
  "module": "src/cli.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build ./src/cli.ts --target=bun --outfile=dist/paldev_cargo_launcher --env=inline --minify --sourcemap=inline",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.4.3"
  }
}
